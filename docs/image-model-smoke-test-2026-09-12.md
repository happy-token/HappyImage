# 图片模型实测：2026-09-12

通过现有登录会话，在线上图片工作区逐个选择当前默认供应商目录中的 8 个模型。
每个模型提交一次文生图、一次带参考图的编辑，共 16 次真实调用；所有任务均到达
“已完成”并显示结果。时间为北京时间 16:47–17:10。

参数：每次 1 张、低质量、1:1；生成白底蓝色圆形，再以测试生成图为参考将圆形改为绿色。
验证的是这些模型入口在本次调用时可用，不保证后续持续可用，也不验证上游实际模型映射。

| 模型 | 文生图 | 图片编辑 | 目录计费类型 |
| --- | --- | --- | --- |
| gpt-image-1 | 成功 | 成功 | 按量 |
| gpt-image-1.5 | 成功 | 成功 | 按量 |
| gpt-image-2 | 成功 | 成功 | 按次 |
| gpt-image-2.5-flare | 成功 | 成功 | 按次 |
| gpt-image-2.5-sunburst | 成功 | 成功 | 按次 |
| codex-gpt-image-2 | 成功 | 成功 | 按次 |
| codex-gpt-image-2.5-flare | 成功 | 成功 | 按次 |
| codex-gpt-image-2.5-sunburst | 成功 | 成功 | 按次 |

2.5 的四个入口都支持图片编辑，无需额外添加 `-edits` 模型名。
这些测试在现网已有版本上执行；新的可用状态记录、故障禁选和 15 秒轮询需要另行发布。
不将这份静态测试报告导入实时状态，以免把旧结果当作持续健康证明。

代码验证：84 项相关后端测试、23 项前端单元测试、TypeScript 检查、Cloudflare 构建通过。
